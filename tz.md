Подумайте какие тесты должны быть

Например, на первую задачу:

Дан лист строк, нужно получить Map<String, Boolean>, где ключ - строка, значение - true,
если строка встретилась более одного раза, иначе false
Copy
тест, который проверит, что при наличии повторяющихся строк в исходном списке Map формируется корректно
тест, который проверит, что при отсутствии повторяющихся строк в исходном списке Map формируется корректно
тест, который проверит, что null список обрабатывается корректно, возвращает пустую Map